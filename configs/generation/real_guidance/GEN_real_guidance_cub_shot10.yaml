exp_name: GEN_real_guidance_cub_shot10
task: generation
seed: 0

data:
  dataset: CUB
  root: ${DIFFDA_ROOT}/datasets
  resolution: 512
  num_classes: 200
  examples_per_class: 10

generation:
  method: RealGuidance
  sd_model: ${DIFFDA_ROOT}/ckpts/stable-diffusion-v1-5
  scheduler: DPMSolver++
  num_inference_steps: 25
  augment_ratio: 5
  strength: 0.1
  root_dir: ${DIFFDA_ROOT}/DiffDA-Eval/generated_images
