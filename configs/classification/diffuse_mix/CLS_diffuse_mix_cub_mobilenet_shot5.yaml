exp_name: CLS_diffuse_mix_cub_mobilenet_shot5
task: classification
seed: 0

data:
  dataset: Birds
  root: /your/path/to/datasets
  resolution: 224
  num_classes: 200
  examples_per_class: 5

generation:
  method: DiffuseMix
  exp: diffuse_mix_cub_shot5_sd15_seed0
  root_dir: /your/path/to/DiffDA-Eval/generated_images

model:
  arch: mobilenet_v3_large
  weights: "IMAGENET1K_V1"

training:
  batch_size: 256
  optimizer: sgd
  lr: 0.01
  momentum: 0.9
  weight_decay: 5e-4
  num_epochs: 128
  criterion: LabelSmoothing
