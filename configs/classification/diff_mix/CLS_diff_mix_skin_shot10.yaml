exp_name: CLS_diff_mix_skin_shot10
task: classification
seed: 0

data:
  dataset: Skin-10
  root: ${DIFFDA_ROOT}/datasets
  resolution: 224
  num_classes: 7
  examples_per_class: -1

generation:
  method: DiffMix
  exp: GEN_diff_mix_skin_shot10_seed0
  root_dir: ${DIFFDA_ROOT}/DiffDA-Eval/generated_images
  utilize_way: FC
  strength: 0.7
  gamma: 0.5

model:
  arch: resnet50
  weights: None

training:
  batch_size: 32
  optimizer: sgd
  lr: 0.01
  momentum: 0.9
  weight_decay: 5e-4
  num_epochs: 100
  criterion: CrossEntropy
