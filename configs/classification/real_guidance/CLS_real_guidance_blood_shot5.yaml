exp_name: CLS_real_guidance_blood_shot5
task: classification
seed: 0

data:
  dataset: Blood-5
  root: /your/path/to/datasets
  resolution: 224
  num_classes: 8
  examples_per_class: -1

generation:
  method: RealGuidance
  exp: real_guidance_blood_sd15_seed0
  root_dir: /your/path/to/DiffDA-Eval/generated_images

model:
  arch: resnet50
  weights: None

training:
  batch_size: 256
  optimizer: sgd
  lr: 0.01
  momentum: 0.9
  weight_decay: 5e-4
  num_epochs: 100
  criterion: CrossEntropy
