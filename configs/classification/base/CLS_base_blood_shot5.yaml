exp_name: CLS_base_blood_shot5
task: classification
seed: 0

data:
  dataset: Blood-5
  root: ${DIFFDA_ROOT}/datasets
  resolution: 224
  num_classes: 8
  examples_per_class: -1

model:
  arch: resnet50
  weights: None

training:
  batch_size: 32
  optimizer: sgd
  lr: 0.01
  momentum: 0.9
  weight_decay: 5e-4
  num_epochs: 100
  criterion: CrossEntropy
