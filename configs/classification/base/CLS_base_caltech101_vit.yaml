exp_name: CLS_base_caltech101_vit
task: classification
seed: 0

data:
  dataset: Caltech101
  root: /your/path/to/datasets
  resolution: 224
  num_classes: 97
  examples_per_class: -1

model:
  arch: vit_b_16
  weights: None

training:
  batch_size: 256
  optimizer: sgd
  lr: 0.03
  momentum: 0.9
  weight_decay: 1e-4
  num_epochs: 100
  criterion: CrossEntropy
