exp_name: CLS_base_caltech101
task: classification
seed: 0

data:
  dataset: Caltech101
  root: ${DIFFDA_ROOT}/datasets
  resolution: 224
  num_classes: 97
  examples_per_class: -1

model:
  arch: resnet50
  weights: None

training:
  batch_size: 32
  optimizer: sgd
  lr: 0.01
  momentum: 0.9
  weight_decay: 1e-4
  num_epochs: 100
  criterion: CrossEntropy
